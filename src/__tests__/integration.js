// import { TransferEventEmitter } from '../api/event/transfer.events';
// import { sdkTransfer } from '../api/utils/sdk';

describe('Integration Testing for zeroBRIDGE', () => {
    it('should transfer funds', () => {
        // TODO - simulate a transfer of funds
        // const transfer = new sdkTransfer(zeroUser, amount, ratio, signer, to, isFast, TransferEventEmitter, dispatch, data);
    });
})