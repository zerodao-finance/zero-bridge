const CardComponent = ({}) => {
return (
    <div>

        </div>
    )
}

CardComponent.defaultProps = {
    props: {
        variant: "default",
        classes: ""
    },
}

export default CardComponent